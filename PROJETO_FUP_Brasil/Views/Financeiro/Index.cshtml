@model IEnumerable<Remake_FUP.Models.Financeiro>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Direcao_Financeira)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descricao_Financeira)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aluno)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Direcao_Financeira)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Descricao_Financeira)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aluno)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id_Financeiro">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id_Financeiro">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id_Financeiro">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
